
# UNIX SHELL

    cd
    ls
        -F (Type)
        -l (Long)
        -s -S (Size)
        -R (Nested)
    pwd
        -p (create mor than one)
    touch
    nano
    rm
    mv file /
    cp file /
    google-chrome

    wc file or * (word count)
    cat file (prints content of files)
    less (similar to cat)
    sort file
    echo (print)

# GIT

    git clone
    git status
    git add
    git commit

    git long


# HTML

    <p>
    <h1>
    <strong>
    <em>vv
    <!--  --> (comment)

# CSS

# FELXBOX

    display: flex (flex container)
    flex
        : 1; (flex: 1; to make divs grow evenly)
        flex-grow
        flex-shrink (0 to keep certain divs from shrinking.)
        flex-basis

        flex: 1 1 auto == flex: auto

        initial 0 1 auto
        auto 1 1 auto
        none 0 0 auto

        align-items: center;
        justify-content: center;

